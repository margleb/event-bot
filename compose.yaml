services:
  event-bot:
    build:
      context: .
    container_name: event-bot
    init: true
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
